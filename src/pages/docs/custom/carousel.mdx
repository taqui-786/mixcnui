---
title: Carousel
---

# Carousel

This is a carousel component support all embla carousel features.

import Badge from "@/components/mdx/Badge";

<div className="flex py-6 flex-row gap-3">
  <Badge label="embla-carousel" />
  <Badge label="Nextjs" />
  <Badge label="Typescript" />
  <Badge label="Tailwindcss" />
</div>

import { PreviewWithCode, Preview } from "@/components/PreviewWithCode";
import {
  CarouselExample,
  CarouselIndicatorExample,
  CarouselOrientation,
  CarouselOrientationWithPlugin,
} from "@/components/extension/CarouselPreview";

<PreviewWithCode>
<Preview className="bg-gray-50">
<CarouselExample />
</Preview>
<Preview toPreview="code">
```sh copy

import {
  Carousel,
  CarouselMainContainer,
  CarouselThumbsContainer,
  SliderMainItem,
  SliderThumbItem,
} from "@/components/extension/carousel";

const CarouselExample = () => {
return (

<Carousel orientation="vertical" className="flex items-center gap-2">
  <div className="relative basis-3/4 ">
    <CarouselMainContainer className="h-60">
      {Array.from({ length: 10 }).map((_, index) => (
        <SliderMainItem
          key={index}
          className="border border-muted flex items-center justify-center h-52 rounded-md"
        >
          Slide {index + 1}
        </SliderMainItem>
      ))}
    </CarouselMainContainer>
  </div>
  <CarouselThumbsContainer className="h-60 basis-1/4">
    {Array.from({ length: 10 }).map((_, index) => (
      <SliderThumbItem
        key={index}
        index={index}
        className="rounded-md bg-transparent"
      >
        <span
          className="border border-muted flex items-center
 justify-center h-full w-full rounded-md cursor-pointer bg-background"
        >
          Slide {index + 1}
        </span>
      </SliderThumbItem>
    ))}
  </CarouselThumbsContainer>
</Carousel>
); };

export default CarouselExample;

````
</Preview>
</PreviewWithCode>

## Installation

import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import ManuallInstallButton from "@/components/mdx/ManuallInstallButton"

<Tabs defaultValue="cli" className="w-full mt-5">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
  ```sh copy
npx mixcn-ui add carousel
````

1. The command will create a `components/mixcnui/Carousel.tsx` in your Nextjs App directory.
2. It will install the neccessary package required for this component.

</TabsContent>
  <TabsContent value="manual">

If you are facing any problem while installation with CLI command try installing Manually.

<ManuallInstallButton href="https://github.com/taqui-786/mixui/blob/main/src/components/mdx/Carousel.tsx"/>
  </TabsContent>
</Tabs>

## Props

import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";

<Table>
  <TableCaption>Keyboard Navigation also support.</TableCaption>
  <TableHeader>
    <TableRow>
      <TableHead>Props</TableHead>
      <TableHead>Type</TableHead>
      <TableHead className="text-right">Default Value</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell className="font-medium">carouselOpitions</TableCell>
      <TableCell>EmblaCarouselOpitions</TableCell>
      <TableCell className="text-right">----</TableCell>
    </TableRow>
    <TableRow>
      <TableCell className="font-medium">orentation</TableCell>
      <TableCell>horizontal | vertical</TableCell>
      <TableCell className="text-right">horizontal</TableCell>
    </TableRow>
  </TableBody>
</Table>

## More Examples

This examples will showcase the use of the carousel component in more advanced use cases, since the simpler use cases are already covered in shadcn doc carousel component.

### Indicator

It is used to show the current slide index and the total number of slides.

<PreviewWithCode className="mt-5">
<Preview className="bg-gray-100">
<CarouselIndicatorExample/>
</Preview>
<Preview toPreview="code">
```sh copy
import {
  Carousel,
  CarouselIndicator,
  CarouselMainContainer,
  CarouselNext,
  CarouselPrevious,
  CarouselThumbsContainer,
  SliderMainItem,
} from "@/components/extension/carousel";

const CarouselIndicatorExample = () => {
return (
<Carousel>
<CarouselNext />
<CarouselPrevious />
<div className="relative ">
<CarouselMainContainer className="h-60">
{Array.from({ length: 5 }).map((_, index) => (
<SliderMainItem key={index} className="bg-transparent">
<div className="outline outline-1 outline-border size-full 
              flex items-center justify-center rounded-xl bg-background">
Slide {index + 1}
</div>
</SliderMainItem>
))}
</CarouselMainContainer>
<div className="absolute bottom-2 left-1/2 -translate-x-1/2">
<CarouselThumbsContainer className="gap-x-1 ">
{Array.from({ length: 5 }).map((_, index) => (
<CarouselIndicator key={index} index={index} />
))}
</CarouselThumbsContainer>
</div>
</div>
</Carousel>
);
};

export default CarouselIndicatorExample;

````
</Preview>
</PreviewWithCode>

### Orientation
The carousel can be oriented horizontally or vertically by setting the orientation prop.

<PreviewWithCode className="mt-5">
<Preview className="bg-gray-100">
<CarouselOrientation/>
</Preview>
<Preview toPreview="code">
```sh copy
import {
  Carousel,
  CarouselMainContainer,
  CarouselNext,
  CarouselPrevious,
  SliderMainItem,
  CarouselThumbsContainer,
  SliderThumbItem,
} from "@/components/extension/carousel";

const CarouselOrientation = () => {
  return (
    <Carousel>
      <CarouselNext className="top-1/3 -translate-y-1/3" />
      <CarouselPrevious className="top-1/3 -translate-y-1/3" />
      <CarouselMainContainer className="h-60">
        {Array.from({ length: 5 }).map((_, index) => (
          <SliderMainItem key={index} className="bg-transparent">
            <div className="outline outline-1 outline-border size-full flex items-center justify-center
            rounded-xl bg-background">
              Slide {index + 1}
            </div>
          </SliderMainItem>
        ))}
      </CarouselMainContainer>
      <CarouselThumbsContainer>
        {Array.from({ length: 5 }).map((_, index) => (
          <SliderThumbItem key={index} index={index} className="bg-transparent">
            <div className="outline outline-1 outline-border size-full flex items-center
            justify-center rounded-xl bg-background">
              Slide {index + 1}
            </div>{" "}
          </SliderThumbItem>
        ))}
      </CarouselThumbsContainer>
    </Carousel>
  );
};

export default CarouselOrientation;

````

</Preview>
</PreviewWithCode>

### Plugin

The carousel component supports all the embla carousel plugins, you can check the embla carousel plugins for more information.

<PreviewWithCode className="mt-5">
<Preview className="bg-gray-100">
<CarouselOrientationWithPlugin/>
</Preview>
<Preview toPreview="code">
```sh copy
import {
  Carousel,
  CarouselMainContainer,
  SliderMainItem,
} from "@/components/extension/carousel";
import AutoScroll from "embla-carousel-auto-scroll";

const CarouselOrientation = () => {
return (
<Carousel
plugins={[
AutoScroll({
speed: 1,
}),
]}
carouselOptions={{
        loop: true,
      }} >
<CarouselMainContainer className="h-60">
{Array.from({ length: 5 }).map((\_, index) => (
<SliderMainItem key={index} className="bg-transparent">
<div className="outline outline-1 outline-border size-full flex 
items-center justify-center rounded-xl bg-background">
Slide {index + 1}
</div>
</SliderMainItem>
))}
</CarouselMainContainer>
</Carousel>
);
};

export default CarouselOrientation;

```
</Preview>
</PreviewWithCode>



